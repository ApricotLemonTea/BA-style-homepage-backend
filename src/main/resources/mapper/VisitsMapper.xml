<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apricotlemontea.homepage.mapper.VisitsMapper">

    <insert id="countPageVisits">
        INSERT INTO hp_daily_visits (visit_date, page, visit_count)
        VALUES (#{date}, #{page}, 1)
        ON DUPLICATE KEY
            UPDATE visit_count = visit_count + 1
    </insert>

    <select id="getOneDayLobbyVisits" resultType="java.lang.String">
        SELECT
            visit_count
        FROM
            hp_daily_visits
        WHERE
            page = 'lobby'
            AND visit_date = #{date}
    </select>

    <select id="getSumLobbyVisits" resultType="java.lang.String">
        SELECT
            sum(visit_count)
        FROM
            hp_daily_visits
        WHERE
            page = 'lobby'
    </select>

    <select id="getVisitsDataList" resultType="com.apricotlemontea.homepage.dto.VisitsData">
        SELECT
            visit_date as date,
            visit_count as todayLobbyVisits
        FROM
            hp_daily_visits
        WHERE
            page = 'lobby'
        ORDER BY
            visit_date
    </select>

</mapper>
